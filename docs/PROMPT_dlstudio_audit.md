# Промпт для нового чата: аудит Studio v2 — найти слабые и плохие места

Только исследование. Ничего не чинить, не рефакторить, не коммитить.
Скопируй блок ниже в новый чат Claude Code в C:\projects\devlogs.

---

Проект — студия производства девлогов/рилсов/трейлеров. Боевая система:
движок **Studio v2** в `common/dlstudio` (контракт `docs/ARCHITECTURE_V2.md`,
493 теста, CLI `dl2`, FastAPI Studio + Vite/TS webui). Легаси `common/devlog`
обслуживает старые проекты (trolley, neotolis_diary). Контекст: `AGENTS.md`
(секция Studio v2), `common/quality/README.md`, `docs/issues/*.md`.

**Задача: независимый аудит свежим взглядом — найти оставшиеся слабые и
плохие места. РЕЖИМ ТОЛЬКО-ЧТЕНИЕ по отношению к проекту: код не менять,
ничего не коммитить; запускать можно только безопасные команды (тесты,
dl2-команды, рендеры) и только во временных папках/скратчпаде.**

Запусти параллельно фоновых сабагентов:

1. **Адверсариальное ревью кода** (Opus, read-only, very thorough) по всему
   `common/dlstudio`: «код неверен, пока не доказано обратное». Каждая
   находка = severity + file:line + конкретный сценарий отказа +
   CONFIRMED/PLAUSIBLE (трассировать код, не спекулировать). Отдельно —
   что проверено и здорово; отдельно — качество тестов (пиняют поведение
   или зеркалят реализацию, чего не хватает). Передай список известных
   отложенных пунктов из `docs/issues/` — их не ре-репортить. Зоны особого
   внимания: стыки модулей (их писали разные агенты), кэш-инвалидация,
   микс-граф `render/assemble.py`, конкурентность (job manager, глобальный
   chunk resolver, параллельный -j), `api/` (безопасность, hot-reload),
   `cli/verify.py` (карта доменов), пути/кодировки на Windows.
2. **Живая эксплуатационная проверка** (Sonnet, real ffmpeg): собрать во
   временной папке полный тестовый проект (VO через lavfi, words.json,
   картинки, музыка) и прогнать весь конвейер как пользователь:
   doctor / check / ir / compose / iter -j / render с музыкой+дакингом+SFX
   (измерить LUFS) / final / publish / verify --full / API через TestClient /
   webui build. Фиксировать всё странное: невнятные ошибки, неожиданные
   пути вывода, расхождения доков с поведением, скорость (холодный vs
   тёплый iter, кэш-хиты, probe-мемо).
3. **Аудит UX/DX и документации** (Sonnet): пройти путь нового пользователя
   по докам — `docs/ARCHITECTURE_V2.md`, `AGENTS.md`, `common/quality/`,
   `.claude/agents/*.md` (включая hook-doctor, music-supervisor,
   publish-packager, deep-reasoner, fast-worker): несуществующие
   команды/флаги/пути, противоречия между документами, отсутствие README у
   `common/dlstudio`, дыры онбординга (что непонятно без этого чата).
4. **Аудит рисков масштаба** (Opus): что сломается первым на реальном
   проекте — 12+ битов, 4-минутное видео, 4K, вертикалки, кириллические
   пути, длинные сессии Studio, диск (рост кэша/мемо), отсутствие
   CI-энфорсмента (тесты запускаются только вручную?).

Сведи в один ранжированный отчёт: критично → высоко → средне → низко,
с доказательствами; отдельным блоком — «проверено и здорово»; отдельным —
качество тестов; отдельным — топ-5 рисков для первого реального ролика на
v2. Ничего не чини — только отчёт.
