# Промпт для нового чата: имплементация Studio v2 (этапы 0–2, ночной режим)

Скопируй блок ниже в новый чат Claude Code в `C:\projects\devlogs`.

---

Реализуй **этапы 0–2** плана `docs/PLAN_STUDIO_V2.md` последовательно —
Studio v2, движок `common/dlstudio`, CLI `dl2`. Я ушёл и до утра не
отвечу: работай полностью автономно, не жди моих ответов ни на одном
шаге.

Контекст:

- `docs/PLAN_STUDIO_V2.md` — утверждённый план на все этапы. Этап 0 =
  12 подтверждённых дефектов (file:line доказательства, требование, тест,
  порядок реализации); этап 1 = шаблон, contact sheet, HyperFrames-мост,
  субтитры, quickstart; этап 2 = skill `dl-make-video`, вычистка v1 из
  agents/skills/доков, stale-feedback защита.
- `docs/ARCHITECTURE_V2.md` — контракт слоёв
  (model → compile → IR → check → render). Прочитать перед началом.
- План уже прошёл независимое ревью против кода 2026-07-16 — полное
  ре-ревью НЕ нужно. Если код разошёлся с описанием конкретного пункта,
  коротко проверь этот пункт и адаптируй фикс, не меняя суть требования.

Правила (обязательны, из §10 плана):

- внутри этапа 0 — строго порядок из «Порядок реализации этапа 0»;
  каждый блок — отдельный коммит; коммиты локальные, **не пушить**;
- каждый фикс начинается с regression-теста, воспроизводящего дефект на
  реальном поведении путей (не мокать `render_beat` нереалистичными
  именами файлов);
- не рефакторить несвязанный код; не трогать `common/devlog` (заморожен);
  никаких fallback-ов на v1 и упоминаний v1 в новых доках/skills;
- блок 2 этапа 0 (0.1 + 0.2 + 0.10 + 0.6) — ОДИН bump версии формата
  cache entry; старые записи считаются промахами, холодные рендеры после
  этого — норма;
- тесты, закреплявшие дефектное поведение (минимум
  `test_cmd_iter_stale_skips_restore_when_file_already_present` и
  no-cache-моки), заменить — это ожидаемо и предусмотрено приёмкой;
- после каждого блока — `dl2 verify --changed`; после этапа 0 — вся
  suite + real-FFmpeg smoke во временной папке с кириллицей и пробелом в
  пути;
- 4K не рендерить; новые зависимости не добавлять.

Ночной режим — обработка проблем без пользователя:

- в конце каждого этапа прогнать его приёмку из плана, закоммитить и
  записать краткий отчёт этапа, затем сразу продолжать следующий;
- если приёмка пункта не проходит или встретился блокер — НЕ жди:
  зафиксируй проблему, отложи только зависимые от неё пункты и продолжай
  всё независимое;
- спорные решения принимай сам по критерию §11 плана (минимальная
  студия, ролик за вечер — при сомнении выбирай более простой вариант) и
  веди отдельный список «решения, принятые без пользователя»;
- деструктивных действий не выполнять: ничего не удалять вне
  scratch/temp, не пушить, main history не переписывать.

Границы:

- приёмка этапа 2 («свежий чат сам проходит workflow») и весь этап 3
  (два реальных ролика) выполняются мной в новых чатах — их НЕ имитируй.
  Твоя зона: подготовить всё для них и самопроверить по механическим
  критериям приёмки (артефакты существуют, verify зелёный, sha256 в
  feedback совпадает).

Финал: сводный отчёт по этапам 0–2 в
`docs/REPORT_dlstudio_etap0-2.md` и в чат — что готово, что заменено в
тестах, результаты smoke, отложенные пункты с причинами, список решений,
принятых без пользователя, и готовый краткий промпт для приёмочного
свежего чата (этап 2/3). После этого остановись.
